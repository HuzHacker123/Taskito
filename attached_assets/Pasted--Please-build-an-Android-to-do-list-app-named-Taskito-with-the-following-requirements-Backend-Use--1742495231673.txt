"Please build an Android to-do list app named Taskito with the following requirements: Backend: Use Python strictly for the backend, leveraging frameworks like Flask or FastAPI. Implement routing, request handling, and APIs for tasks, users, notifications, and authentication. Use SQLAlchemy or another Python ORM for database management (SQLite/PostgreSQL) to store tasks, user information, and rewards. Ensure proper error handling in the backend to handle invalid input, database issues, and API errors. Frontend: Design the app’s theme to resemble the clean and elegant style of an iPhone UI. Ensure the GUI is highly user-friendly and visually appealing. 
Features: The app should open with a mandatory user login screen, allowing users to log in using email or Google account. Enable the creation, editing, and deletion of tasks. The following fields should be mandatory: Task Title (required). Task Description, Date, Time, Priority, and Sub-tasks (all must be provided). 
Create filters for task categories: Work, Personal, and Custom (with a '+' sign for creating new custom categories). Include task priority settings (e.g., High, Medium, Low). Allow daily recurring tasks with a toggle option. Track task progress for user-selected tasks and show progress status. Build a dynamic dashboard displaying tasks with options to filter and view progress. 
Mark completed tasks, which should remove them from the dashboard but retain their data for achievement tracking. Display a user’s task achievements on the dashboard, rewarding them with trophies for completing tasks. Enable notifications for task reminders. Allow the user to set the reminder time in advance and take necessary permissions (notifications, calendar access, etc.). Provide a visually engaging UI for achievements and progress tracking. Setup: Ensure that the app’s codebase is fully compatible with VS Code as the primary development and execution environment. Provide clear instructions and configurations (such as a requirements.txt or pipfile) for setting up the project, running the backend server, and connecting it to the frontend. Include detailed setup documentation for developers, specifying steps to install necessary extensions, dependencies, and tools in VS Code. Additional Considerations: Ensure error handling across the app, including invalid input and task operations. Add other useful features as necessary to enhance the user experience. Please deliver the app with a fully functional backend in Python, a polished frontend, and a well-structured database, ensuring seamless execution in VS Code."* there should be an option on the dashboard to check your achievements of completed tasks and the user should be rewarded with trophies for no. of tasks completed
Add these features
. Should have a feature called task progress for the user-selected task so that one can track progress for those specific tasks
. Should have a dynamic dashboard
. Once the user marks the task completed the task should vanish from the dashboard but the data should be stored of the task .
. there should be an option on the dashboard to check your achievements of completed tasks and the user should be rewarded with trophies for no. of tasks completed
.the reminder sends notificaion at the time of the given deadline.
